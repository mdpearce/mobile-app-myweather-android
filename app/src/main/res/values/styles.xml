<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="LabelStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:padding">@dimen/cell_padding</item>
    </style>

    <style name="DataStyle" parent="TextAppearance.AppCompat.Body2">
        <item name="android:padding">@dimen/cell_padding</item>
        <item name="android:layout_marginLeft">@dimen/cell_margin</item>
    </style>

</resources>
